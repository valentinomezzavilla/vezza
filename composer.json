{
    "name": "vezza/panel",
    "description": "Panel admin de VEZZA (Composer solo para tests)",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "config": {
        "sort-packages": true
    }
}
